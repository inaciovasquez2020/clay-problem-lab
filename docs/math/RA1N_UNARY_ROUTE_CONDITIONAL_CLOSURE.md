# RA1n Unary Route Conditional Closure

Status: CONDITIONAL.

This document records the strongest admissible consequence of the selected unary Taylor-remainder route.

Canonical unary surface:

\widehat G_k(ξ)=φ(2^{-k}ξ)|ξ|^{-4}.

P_k f(ξ)=f(ξ_k)+∇f(ξ_k)·(ξ−ξ_k).

r_k(ξ)=\widehat G_k(ξ)−P_k\widehat G_k(ξ).

Locked upstream facts:

1. The corrected RA1n coercivity scale is D_k := 2^k∇_ξ.
2. The downstream shell-product kernel route is compatible only with normalized derivatives.
3. For the selected unary Taylor-remainder model,

sup_{|ξ|~2^k}|(2^k∇_ξ)^α r_k(ξ)| ≤ C_α 2^{-4k} ≤ C_α.

Conditional conclusion:

For the selected unary Taylor-remainder RA1n surface, the normalized symbol-derivative obstruction is closed.

Therefore the shell-product kernel estimate is admissible for the selected unary surface.

Boundary:

This does not close full RA1n unless the repository declares the selected unary surface to be the sole canonical RA1n first-moment surface.

The following remain non-closed for full RA1n:

1. Binary residuals r_k(ξ,η), r_k(η,ζ).
2. Ternary residuals r_k(ξ,η,ζ).
3. Transverse high-high residuals.
4. Sampled surrogate residuals.
5. Conditional symbolic residuals not equal to the selected unary Taylor remainder.

No full RA1n closure claim is admissible until the canonical-surface selection gate is resolved.

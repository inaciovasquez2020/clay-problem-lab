# DDYO RA1n Symbol Consistency Blocker

Conditional.

Blocked objects:
1. Exact DDYO kernel formula for \(G_j(x)\).
2. Exact DDYO remainder-symbol formula for \(r_k(\xi)\).

Current public inconsistency:
- `docs/status/DDYO_RA1N_RK_DEFINITION_STATUS.md` declares
  \[
  r_k=\widehat G_k-P_k\widehat G_k
  \]
  canonical.
- `docs/math/DDYO_RA1N_EXACT_MISSING_LEMMA.md` states the exact missing lemma
  must be derived without using the unsupported replacement
  \[
  r_k=\widehat G_k-P_k\widehat G_k.
  \]

Required normalization step:
- do not replace any remaining occurrence of \(G_j(x)=2^{2j}\Gamma(2^j x)\) by another formula
  until the exact DDYO kernel formula is promoted into a canonical repository file.
- do not lock any canonical RA1n derivative theorem until the exact DDYO \(r_k(\xi)\)
  is promoted into a canonical repository file.

No further progress possible without new input.

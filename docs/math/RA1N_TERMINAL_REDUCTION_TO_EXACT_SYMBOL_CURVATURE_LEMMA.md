# RA1n Terminal Reduction to Exact Symbol Curvature Lemma

## Status
CONDITIONAL

## Reduction statement
Assume the Exact Symbol Curvature Lemma for terminal_high_high_resonance_curvature_gain:

There exist constants c > 0, alpha >= 0, a localized high-high support region Omega_k, and an exact closed-form DDYO interaction symbol m_k(xi,eta) for
r_k = \widehat G_k - P_k \widehat G_k
such that, for the transverse renormalized symbol
\widetilde r_k(xi,eta) := m_k(xi,eta) - m_k(xi,eta)|_{xi || eta},
one has
|\widetilde r_k(xi,eta)| >= c 2^{-alpha k} |xi \wedge eta|^2
for all (xi,eta) in Omega_k.

Then the named next missing object terminal_high_high_resonance_curvature_gain is discharged.

## Consequence
Under that input, the terminal frontier simulator may change status from OPEN to CLOSED only after replacing the current placeholder scoring rule by the exact closed-form symbol evaluation on the localized high-high support.

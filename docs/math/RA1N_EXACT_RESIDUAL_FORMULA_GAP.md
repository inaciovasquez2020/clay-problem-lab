# RA1n Exact Residual Formula Gap

Status: OPEN.

Current locked scale target:

D_k := 2^k∇_ξ.

Current remaining theorem:

sup_{|ξ| ~ 2^k} |(2^k∇_ξ)^α r_k(ξ)| ≤ C_α.

Formal obstruction:

The corrected scale-normalized RA1n layer is not enough by itself. To continue toward theorem-level closure, the repository must identify the exact source formula for

r_k(ξ) = \widehat G_k(ξ) − P_k \widehat G_k(ξ)

and the exact action of P_k on \widehat G_k.

Weakest missing object:

A source-backed definition of P_k \widehat G_k that permits direct computation of

(2^k∇_ξ)^α r_k(ξ)

on |ξ| ~ 2^k.

No RA1n closure claim is admissible until that exact formula is fixed.

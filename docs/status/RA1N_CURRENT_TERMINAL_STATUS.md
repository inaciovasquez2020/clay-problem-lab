# RA1n Current Terminal Status

Status: CONDITIONAL.

Current terminal state:

RA1n has a closed unary closure lane, not a full RA1n closure.

Closed lane:

RA1n-unary-closure-lane.

Canonical unary surface:

\widehat G_k(ξ)=φ(2^{-k}ξ)|ξ|^{-4}.

P_k f(ξ)=f(ξ_k)+∇f(ξ_k)·(ξ−ξ_k).

r_k(ξ)=\widehat G_k(ξ)−P_k\widehat G_k(ξ).

Locked unary theorem:

sup_{|ξ|~2^k}|(2^k∇_ξ)^α r_k(ξ)| ≤ C_α 2^{-4k} ≤ C_α.

Repository status:

The normalized symbol-derivative obstruction is closed for the selected unary Taylor-remainder surface.

Full RA1n remains conditional because the repository still contains noncanonical binary, ternary, transverse high-high, sampled surrogate, and mixed conditional symbolic residual surfaces.

Terminal obstruction:

Full RA1n closure requires either:

1. a theorem proving that the RA1n first-moment shell-product theorem uses only the unary closure lane; or
2. separate theorem-level closure for every noncanonical residual surface.

No full RA1n closure claim is made.
